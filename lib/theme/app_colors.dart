import 'package:flutter/material.dart';

/// Bảng màu chung cho toàn bộ ứng dụng Vietnam Explore.
class AppColors {
  AppColors._();

  // === Primary ===
  static const Color primary = Color(0xFFE06040);

  // === Background ===
  static const Color background = Color(0xFFF7F3EE);

  // === Text ===
  static const Color text = Color(0xFF1A1A1A);

  // === Grey ===
  static const Color greyBorder = Color(0xFFE0E0E0);
  static const Color greyArrow = Color(0xFF9E9E9E);
}
